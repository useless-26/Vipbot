[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["mkdir -p static/css static/js templates"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2"